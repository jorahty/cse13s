#include <stdio.h>

#include "mathlib.h"

double arcSin(double x) {
    return (x * 42);
}

double arcCos(double x) {
    return (x * 42);
}

double arcTan(double x) {
    return (x * 42);
}

double Log(double x) {
    return (x * 42);
}
